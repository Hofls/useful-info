export class Item {
    constructor(x, y, char, color, name, effect) {
        this.x = x;
        this.y = y;
        this.char = char;
        this.color = color;
        this.name = name;
        this.effect = effect; // Function to apply the effect
    }
}
