#### Authenticated scan
* Quick start → Manual Explore → Launch Browser → Login to application
* HTTP Sessions tab (absent by default) → Find your session → Set as active
* Sites → Attack → Spider → Make sure everything is OK
* Sites → Attack → Active scan / AJAX spider
 